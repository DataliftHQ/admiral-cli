<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admiral - CLI Authentication</title>
    <style>
        :root {
            --color-white: #ffffff;
            --color-primary: #0d7377;
            --color-primary-light: #f0fdfa;
            --color-gray-50: #f8fafc;
            --color-gray-200: #e2e8f0;
            --color-gray-500: #64748b;
            --color-gray-900: #0f172a;
            --color-error: #dc2626;
            --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            --shadow-xl:
                0 0 0 1px rgba(13, 115, 119, 0.03),
                0 1px 2px 0 rgba(15, 23, 42, 0.04),
                0 4px 8px -2px rgba(15, 23, 42, 0.06),
                0 12px 24px -4px rgba(15, 23, 42, 0.08);
        }
        @media (prefers-color-scheme: dark) {
            :root {
                --color-white: #131c2e;
                --color-primary: #2dd4bf;
                --color-primary-light: #0c1f1e;
                --color-gray-50: #020617;
                --color-gray-200: #1e293b;
                --color-gray-500: #64748b;
                --color-gray-900: #e2e8f0;
                --color-error: #f87171;
                --shadow-xl:
                    0 0 0 1px rgba(45, 212, 191, 0.05),
                    0 1px 2px 0 rgba(0, 0, 0, 0.3),
                    0 4px 8px -2px rgba(0, 0, 0, 0.4),
                    0 12px 24px -4px rgba(0, 0, 0, 0.5);
            }
        }
        *, *::before, *::after { box-sizing: border-box; }
        html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
        body {
            margin: 0;
            font-family: var(--font-sans);
            font-size: 1rem;
            line-height: 1.5;
            color: var(--color-gray-900);
            background: linear-gradient(135deg, var(--color-gray-50) 0%, var(--color-primary-light) 50%, var(--color-gray-50) 100%);
            min-height: 100vh;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            padding-top: 12vh;
        }
        .card {
            background: var(--color-white);
            width: 100%;
            max-width: 480px;
            border-radius: 1rem;
            border: 1px solid var(--color-gray-200);
            box-shadow: var(--shadow-xl);
            padding: 3rem 2rem;
            text-align: center;
            margin: 0 1.5rem;
        }
        .logo {
            margin-bottom: 2rem;
        }
        .logo img {
            height: 32px;
            width: auto;
        }
        @media (prefers-color-scheme: dark) {
            .logo img {
                filter: invert(1);
            }
        }
        .error-circle {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 3px solid var(--color-error);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
        }
        .error-circle svg {
            width: 40px;
            height: 40px;
            fill: var(--color-error);
        }
        h1 {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--color-gray-900);
            margin: 0 0 0.75rem;
        }
        p {
            font-size: 0.875rem;
            color: var(--color-gray-500);
            margin: 0;
            line-height: 1.6;
        }
        p.error-message {
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
<div class="card">
    <div class="logo">
        <img src="https://assets.admiral.io/email/admiral-black-logo-email.png" alt="Admiral">
    </div>
    <div class="error-circle">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
            <path d="M23.954 21.03l-9.184-9.095 9.092-9.174L21.03-.046l-9.09 9.179L2.764.045l-2.81 2.81L9.14 11.96.045 21.144l2.81 2.81 9.112-9.192 9.18 9.1z"/>
        </svg>
    </div>
    <h1>Authentication failed</h1>
    <p class="error-message">{{.Message}}</p>
    <p>You may close this tab and return to the terminal.</p>
</div>
</body>
</html>
