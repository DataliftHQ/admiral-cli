<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admiral - CLI Authentication</title>
    <style>
        :root {
            --color-white: #ffffff;
            --color-primary: #0d7377;
            --color-gray-200: #e2e8f0;
            --color-gray-500: #64748b;
            --color-gray-900: #0f172a;
            --color-success: #16a34a;
            --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            --shadow-xl:
                0 0 0 1px rgba(13, 115, 119, 0.03),
                0 1px 2px 0 rgba(15, 23, 42, 0.04),
                0 4px 8px -2px rgba(15, 23, 42, 0.06),
                0 12px 24px -4px rgba(15, 23, 42, 0.08);
        }
        @media (prefers-color-scheme: dark) {
            :root {
                --color-white: #131c2e;
                --color-primary: #2dd4bf;
                --color-gray-200: #1e293b;
                --color-gray-500: #64748b;
                --color-gray-900: #e2e8f0;
                --color-success: #4ade80;
                --shadow-xl:
                    0 0 0 1px rgba(45, 212, 191, 0.05),
                    0 1px 2px 0 rgba(0, 0, 0, 0.3),
                    0 4px 8px -2px rgba(0, 0, 0, 0.4),
                    0 12px 24px -4px rgba(0, 0, 0, 0.5);
            }
            body {
                background:
                    radial-gradient(ellipse at 50% 0%, rgba(148, 163, 184, 0.04) 0%, transparent 55%),
                    linear-gradient(135deg, #0a0f1e 0%, #0c1220 45%, #090e1c 100%);
            }
        }
        *, *::before, *::after { box-sizing: border-box; }
        html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
        body {
            margin: 0;
            font-family: var(--font-sans), serif;
            font-size: 1rem;
            line-height: 1.5;
            color: var(--color-gray-900);
            background: linear-gradient(135deg, #f8fafc 0%, #f0fdfa 50%, #e0f2fe 100%);
            min-height: 100vh;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            padding-top: 12vh;
        }
        .card {
            background: var(--color-white);
            width: 100%;
            max-width: 480px;
            border-radius: 1rem;
            border: 1px solid var(--color-gray-200);
            box-shadow: var(--shadow-xl);
            padding: 3rem 2rem;
            text-align: center;
            margin: 0 1.5rem;
        }
        .logo {
            margin-bottom: 2rem;
        }
        .logo img {
            height: 32px;
            width: auto;
        }
        @media (prefers-color-scheme: dark) {
            .logo img {
                filter: invert(1);
            }
        }
        .success-circle {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 3px solid var(--color-success);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
        }
        .success-circle svg {
            width: 40px;
            height: 40px;
            fill: var(--color-success);
        }
        h1 {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--color-gray-900);
            margin: 0 0 0.75rem;
        }
        p {
            font-size: 0.875rem;
            color: var(--color-gray-500);
            margin: 0;
            line-height: 1.6;
        }
    </style>
</head>
<body>
<div class="card">
    <div class="logo">
        <img src="https://assets.admiral.io/email/admiral-black-logo-email.png" alt="Admiral">
    </div>
    <div class="success-circle">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
            <path d="M20.285 2l-11.285 11.567-5.286-5.011-3.714 3.716 9 8.728 15-15.285z"/>
        </svg>
    </div>
    <h1>Authentication successful!</h1>
    <p>You may now close this tab and return to the terminal.</p>
</div>
<script>
    // Attempt to close the tab after a short delay.
    // Browsers block window.close() on tabs not opened via window.open(),
    // which is the case for CLI-initiated OAuth2 flows. The call is best-effort;
    // when it fails the user sees the "close this tab" message above.
    setTimeout(function () { window.close(); }, 3000);
</script>
</body>
</html>
